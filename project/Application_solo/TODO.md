# Gravity Shooter (仮) 実装TODOリスト

以下のタスクは仕様書（GameDesignDocument.md）の要件に基づき、エンジン側（IrufemiEngine）とゲーム側（Application_solo）での実装項目を整理したものです。

## 1. コアゲームプレイ＆操作基盤
- [x] **スプライン曲線のルート進行システム**
  - [x] 空間上の見えないルート（スプライン曲線）に沿って自機を自動前進させるパス移動コンポーネントの実装
- [x] **自機のオフセット移動（相対移動）制御とアクション表現**
  - [x] 親子階層化（PlayerCart -> Player）による、レール進行とオフセット移動の処理分離
  - [x] XInput/DirectInputによるゲームパッド（左スティック）とWASDキーの移動入力ベクトル取得と正規化
  - [x] 慣性（Acceleration）と摩擦（Friction）による物理ベースの疑似アクション挙動の導入
  - [x] 移動速度に応じた進行方向への傾き（ロール回転 / バンキング）による重力ターンの表現
  - [x] サイン波を用いた常時浮遊アニメーション（Hovering）の適用
  - [x] 移動範囲のクランプ（壁抜け防止）
- [x] **手動マーキング式マルチロックオンシステム (`PlayerTargetingComponent`)**
  - [x] 右クリック（またはパッド専用ボタン）による手動マーキング（ターゲットのキュー固定化）の実装
  - [x] 同一ターゲットへの「重複ロックオン」の許容（所持ガレキ数を上限とする）
  - [x] 2D距離＋3D深度によるスコアリングエイム判定の導入
  - [x] `CollisionManager::Raycast` を用いた遮蔽物（壁裏）ロックオン防止判定の導入
  - [x] Rキー（手動キャンセル）によるキュー全クリア処理の実装
  - [x] 重複ロック時の「サテライト方式（周囲に展開する）」UIアニメーションの実装

## 2. アクション：重力スロー（ステートマシン）
- [x] **自機のアクションステートマシン構築**
  - [x] ボタンのPress（単発押し）をトリガーとする状態遷移の作成
- [x] **状態1: [引き寄せ開始]**
  - [x] 周囲のガレキを検知し、自機に向かって移動させる
  - [x] ロックオン、またはエイム（ホバー）中のボスのシールドを優先して奪う直感的な強奪機能
  - [x] （物理演算を使わないアプローチの採用による）引き寄せ・浮遊中の位置同期処理
- [x] **状態2: [自機周辺で保持]**
  - [x] 自機を親（ローカル座標）とした `sin/cos` による軽量な浮遊アニメーションの実装
- [ ] **状態3: [ロックオン対象へ一斉掃射]**
  - [x] ターゲットへのホーミング（追従）処理
  - [x] 発射後の簡易コリジョン（Sphere等によるHit判定）への切り替え
  - [x] 投げたガレキ同士（弾とシールド）が命中した際の、相殺・破壊（シールド剥がし）処理の実装
  - [x] 左クリックで一斉掃射モードへ移行し、0.15秒間隔のディレイでガレキを連続発射する処理
  - [x] キューが空（未マーキング）の場合、レティクル奥の空間へ向けて直進（ノーロック射撃）させる処理

## 2.5 高度なキャラクターアニメーション (Humanoid / 動きの掛け算)
※これらの実装には、事前にエンジン側(IrufemiEngine)の機能拡張（Phase 4）が必要となります。
- [ ] **2D Blend Tree を用いた姿勢制御**
  - [ ] 左スティックの入力 (X, Y) をエンジンに渡し、待機（ホバー）と前後左右への傾き・スラスター移動モーションを滑らかに無段階ブレンドする処理。
- [ ] **ボーンマスク（上半身/下半身分離）によるアクションの掛け算**
  - [ ] 移動や緊急回避（下半身・ベース部分）のアニメーションを維持したまま、ガレキの引き寄せや投擲アクション（上半身・腕）を上書きブレンドして再生する。
- [ ] **加算ブレンド（Additive Blending）を用いた緊急回避**
  - [ ] 現在の飛行姿勢（前傾や横傾き）をベース姿勢とし、緊急回避（バレルロールなど）のモーション差分のみを加算することで、いかなる状態からでも破綻なく回避行動を行う。
- [ ] **Look-At IK / Hand IK による視覚的説得力の向上**
  - [ ] 手動マーキングしたロックオン対象に対し、常に顔や目線を向け続ける（Look-At IK）。
  - [ ] 周囲に浮遊させているガレキに対し、手やエネルギーの触手を正確にかざす・掴む表現（Hand IK）。

## 3. 最適化・メモリ管理 (Engine / Application)
- [x] **Data-Orientedなターゲット管理 (TargetableComponent)**
  - [x] 毎フレームの全オブジェクト再帰検索を廃止し、自己登録型コンポーネントを用いた定数時間ループ処理へ移行
- [x] **Componentのライフサイクル導入**
  - [x] `OnEnable` / `OnDisable` を実装し、オブジェクトプール等のアクティブ切り替えに連動する安全なイベントフックを整備
- [ ] **Object Poolパターンの実装と適用**
  - [x] C++テンプレートを用いた再利用可能な `ObjectPool<T>` クラスの実装
  - [x] ガレキ、ザコ敵の Object Pool 適用（`VirtualEntityManager`等の活用）
  - [ ] `DebrisManager` のバリエーション対応（複数種類のガレキプレハブのリスト管理と、Prefab単位での独立した Object Pool の構築）
  - [ ] ボスの弾幕用データ構造体の定義
  - [ ] ボス戦初期化時の弾幕ObjectPoolの事前メモリ割り当て（スパイク防止）
  - [x] `EnvironmentManagerComponent` の作成と本番シーン(`GameScene`)への組み込み（テスト用建造物の動的配置）
  - [x] 投擲して外れたガレキの自機からの距離監視と限界距離超過時のプール返却処理の実装
    - 限界距離に到達したガレキは即座に親から `RemoveChild` せず、`DebrisManager` の `pendingReleases_`（削除キュー）に積み、Update終端で安全に遅延削除（Pending Kill）する機構を導入済み。
  - [x] 建造物（背景）のOBBコライダーへの衝突検知によるプール返却処理の実装
  - [ ] ガレキ回収時に VoxelParticle を放出させ、風や物理挙動に合わせて散る演出の実装
- [x] **Data-Orientedな背景環境の構築（インスタンシングへの移行）**
  - [x] 現在 `EnvironmentManagerComponent` で GameObject の親子付けとして動的生成している柱や壁などの静的背景を、`ModelBatchRendererComponent` を用いたインスタンシング描画基盤へ移行する。
  - [x] プレハブリンク機能によりエディタ上のRaycast選択（操作性）を維持したまま、描画時のみ自動でバッチ描画へすり替えるAAA基準の最適化を完了。
- [x] **DX12 Instancing / Batching 描画の対応**
  - [x] 無数のガレキや破片を低負荷で描画するためのインスタンシング描画基盤の構築（※既存の描画エンジンの拡張）
  - [x] 保守的GPUカリング(Conservative GPU Culling)の導入による、10,000個超のガレキ描画時のフレームレート安定化とバリデーションエラーの完全修正
  - [x] マルチロックオン時のレティクル等、UI（Sprite）の大量描画に向けた SpriteBatch（またはUIインスタンシング）の実装
- [ ] **物理判定の最適化 (Engine連携)**
  - [ ] エンジン側で実装予定の TLAS & BLAS 動的BVH (および Convex Hull) に対応した大量ガレキの衝突判定の組み込み
- [x] **フレームドロップ最適化 (Async & Zero Allocation)**
  - [x] 非同期レイキャスト (Async Raycast) の導入による視線判定のマルチスレッド化
  - [x] 毎フレーム生成されるコンテナのメンバ変数化（Zero Allocation化）によるGC負荷の根絶

## 4. データ駆動設計 (Editor / Application)
- [ ] **外部データ（CSV/JSON）読み込み機構**
  - [ ] nlohmann/json などのライブラリ導入とWaypoint/Enemy構造体とのパース処理
  - [ ] ゲーム起動時のファイル読み込みと、スプライン制御点への適用処理
  - [ ] ウェーブ進行を管理する WaveManager の実装
  - [ ] JSONデータに基づく特定座標への敵の遅延スポーン処理
  - [x] `EnvironmentManagerComponent` 内にハードコードされている柱や壁などの環境物配置ルールを、プロパティ（外部JSONデータ）からの読み込み（データ駆動設計）へ移行し脱ハードコード化を完了。
- [ ] **エディタ連携**
  - [ ] ImGuiを用いたエディタ側でのウェイポイント配置・調整機能の作成

## 5. 描画・ビジュアル演出
- [ ] **ポストプロセス演出の拡充**
  - [ ] Color Grading による重厚な空気感の演出（ルックアップテーブル等の適用）
  - [ ] Bloomエフェクトの調整（青白いエネルギー発光表現の最適化）
- [x] **自機と敵のエネルギー色の区分け（ビジュアルアイデンティティの確立）**
  - [x] **自機（Player）**: スマートエネルギー。シアン（青緑）、ホワイト、ネオンブルーを基調とし、洗練された制御下にあるエネルギーを表現。プレイヤーが引き寄せたガレキのオーラはシアンに発光させる。
  - [x] **敵（Boss/Enemy）**: ダーク・重厚エネルギー。ダークパープル、マゼンタ、クリムゾンを基調とし、重く破壊的で混沌としたエネルギーを表現。ボスのビームやボス所有のガレキはマゼンタ/ダークパープルに発光させる。

## 6. コードレビュー修正（07/03指摘分）
- [ ] **アーキテクチャ・設計の改善**
  - [x] `BossComponent` の状態遷移（enum + if文）に State パターンを適用し、状態ごとの振る舞いをクラスとして分離する
  - [ ] `EffectManagerComponent` などの静的インスタンス管理（シングルトン）を排除し、DIまたはエンジン側のマネージャ経由の管理へ移行する
  - [ ] 責務が集中している長大なクラス・関数（Update等）の分割・移譲を行う
- [x] **安全性とパフォーマンスの向上**
  - [x] `BossComponent.cpp`, `DebrisComponent.cpp` などのマジックナンバーをメンバ変数化（プロパティ化）し、JSON/Editorから調整可能にする
- [ ] **コード規則・構成の整理**
  - [x] `Application_solo/components` 内のファイル数が増加している場合、機能単位でサブフォルダに分割して管理する（Boss関連対応済）

## 7. 追加タスク（ボス・ドローン・弾幕の構造リファクタリング）
- [x] **ボスのコンポーネント構造の最適化とカプセル化**
  - [x] `BossContainer` 直下にあった複数のバッチ描画用コンポーネント（ModelBatchRendererComponent）の競合を防ぐため、専用の子オブジェクトとして `BossDroneManager` と `BossBulletManager` を分離。
  - [x] 欠落していた `DroneManagerComponent`, `BossBulletManagerComponent` などのファクトリへの登録処理を追加。
  - [x] `DroneManagerComponent::DeployDrones` に残っていた古い「`DroneManager` フォルダを動的生成する」ハードコードを削除し、プレハブを直接 `BossDroneManager` の子オブジェクトとして追加するように修正。
- [ ] **ドローン・弾幕システムの実装仕上げ（やり残しタスク）**
  - [x] **ボスの弾幕の当たり判定（DOD）実装**
    - `BossBulletManagerComponent::Update()` において、仮想エンティティ（実体のない弾）とプレイヤーの距離（Distance）を毎フレーム計算し、一定距離以下なら被弾とする高速な当たり判定処理を追加する。（実装完了済）
  - [x] **エディタ上でのアクティブ数表示の修正**
    - `DroneManagerComponent::Update()` に `activeDroneCount_ = activeDrones_.size();` を追加し、Inspector上の `Active Drones (Batch)` プロパティが常に最新の値を表示するように動的更新する。

## 8. レベルデザインと演出の拡張（将来の課題・就活版以降の対応）
- [ ] **ボス戦のレール終端（行き止まり）におけるアリーナバトルへのフェーズ移行 (Cornered Beast)**
  - 現在の実装（A案: 超長距離レールによるDPSチェック / 逃げ切られる前に倒す）をベースとしつつ、将来的な拡張として実装を検討する。
  - レールの終点に到達した際、自機（PlayerCart）の強制前進をストップさせる機能の追加。
  - ボスが逃走（相対追従）をやめて反転し、プレイヤーの真正面（カメラの焦点）に陣取る「最終決戦モード（固定砲台化など）」へステートを切り替える処理の実装。
  - プレイヤーは前進せず、ホバーによる上下左右の回避行動のみでボスと撃ち合う（ドッジボールのような死闘感の演出）。