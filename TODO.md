# 今後の実装・拡張タスク (TODO / BACKLOG)

このファイルは、授業資料（`gakkousuraido` 等）に基づいた拡張タスクや、今後対応が必要となるアーキテクチャの変更、機能実装のバックログをまとめるためのものです。

## 1. プリミティブと頂点フォーマットの拡張 (Ring等の高度な表現)

### `VertexData` 構造体への頂点カラーの追加
- **概要**: 現在の `VertexData` 構造体に「頂点カラー（Color）」情報を追加する。
- **実装要件**: Ringの内側・外側に対する独立したカラー指定や、開始・終了地点におけるα値のフェード（グラデーション）を実現する。
- **影響範囲**: 
  - `VertexData`（`BaseResource.h` 等）の構造変更
  - 全てのエフェクト用シェーダー・パイプラインの頂点入力レイアウト（`D3D12_INPUT_ELEMENT_DESC`）の修正
  - （※影響が大きいため、通常描画用とエフェクト/パーティクル用の独自の頂点フォーマットとして分離する設計も検討する）

### 開始・終了半径の動的制御（スプライン分割等）
- **概要**: プリミティブ生成時に、単一の静的な半径だけでなく、長さ方向に対する半径の変化を行えるようにする。
- **実装要件**: 端点と中間角度の点でスプライン曲線等を作成・分割し、徐々に太くなる/細くなるなどの形状を表現可能にする。外径のみ変更するケースが多用される想定。

## 2. ParticleSystem のアーキテクチャ改良

### プリミティブの統合
- **概要**: 現状のParticleSystemにおいて通常のBillboard（Plane）だけでなく、生成したRingなどの「各種Primitive」をParticleとして放出・描画できるように対応させる。

### パラメータアニメーションの拡充
- **概要**: Transformや先述の頂点情報（色、太さ）などの各種パラメータに対して、時間経過をもとにした**簡易的なAnimation**を付与できる仕組みを構築する。
- **必要性**: ゲーム画面上でモノの「動き」を持たせ、エフェクトを通じたリッチな表現やアクセントを追加するため。
- **注記事項**: 特に **UV Scroll** の積極的な活用が強く推奨されているため、シェーダーまたは定数バッファ(CBuffer)側から容易にUVスクロールを制御できる機構を整えること。
