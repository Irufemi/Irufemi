# 🎮 IrufemiEngine コンポーネント指向エディタ構築計画

## ✅ フェーズ1: コンポーネント指向アーキテクチャの完成
ECSライクな柔軟なエンティティ管理システムの構築。
- [x] すべてのエンティティのベースとなる `GameObject` クラスの作成
- [x] `GameObject` にアタッチできる `Component` 基底クラスの作成
- [x] 基本コンポーネント群（`Transform`, `MeshRenderer`, `PrimitiveRenderer`, `SpriteRenderer`, `Collider`等）の実装
- [x] 【UI分離】コンポーネントから ImGui 依存を完全に排除（`ComponentEditorRegistry` への移行）
- [x] 【Fat Class解消】`ObjClass` 等の内部トランスフォーム依存を排除し、コンポーネント主導の描画へ移行

## ✅ フェーズ2: エディタ基盤機能の構築
エディタとしての基本的な操作画面とオブジェクト操作の実装。
- [x] `EditorManager` によるエディタ機能の統括（`#ifdef EditorMode`）
- [x] 「ヒエラルキー（Hierarchy）パネル」による全GameObjectのツリー管理
- [x] 「インスペクター（Inspector）パネル」によるコンポーネントパラメータの動的編集
- [x] `ImGuizmo` を使用したシーンビュー上での直感的なトランスフォーム操作
- [x] 「シーンビュー（SceneView）パネル」による描画結果のプレビューとデバッグ表示切替

## 🖱️ フェーズ3: エディタの高度化と操作性の向上（現在進行中）
より「ツール」として使いやすく、堅牢なシステムへの昇華。
- [x] **コマンドパターンによる Undo/Redo システムの実装**
    - [x] `ICommand` インターフェースと履歴スタックの構築
    - [x] Transform（座標・回転・スケール）および名前変更の Undo 対応
    - [x] オブジェクトの生成・削除・コンポーネント着脱の Undo 対応
- [x] **プロジェクトブラウザ（ProjectBrowser）の最適化（フェーズ1完了）**
    - [x] 毎フレームのファイルスキャンを廃止し、メモリキャッシュを用いた明示的なリフレッシュ（手動更新）アーキテクチャへの移行。
- [x] **自動ファイル監視システム (DirectoryWatcher) の構築**
    - [x] Windows API (`ReadDirectoryChangesW`) を利用し、エディタ外でのファイル追加・変更を検知して自動で `RefreshCache()` を発火させるバックグラウンド監視機構の導入。
- [x] **ピッキング機能の精緻化**
    - シーンビュー上で直接オブジェクトをクリックして選択する際の判定精度向上（アウトライン表示の追加）。
- [x] **アセットのドラッグ＆ドロップ連携の強化**
    - [x] プロジェクトブラウザからシーンビューへのモデル/テクスチャのD&Dによるインスタンス生成・割当。

## 💾 フェーズ4: シーン管理とシリアライズ
エディタで作成した世界を永続化し、ゲームとして実行する。
- [x] `GameObject` / `Component` の JSON シリアライズ・デシリアライズ基盤の実装
- [x] **エディタ上でのシーン保存・読込UI（File Menu）の実装**
- [x] **起動時の初期シーン自動ロード・自動生成機能**
- [x] プレハブ（Prefab）システムの導入（GameObjectの構成をテンプレート化して保存）

## 🔄 フェーズ5: ランタイム連携とデプロイ
エディタと実際のゲーム実行環境の統合。
- [x] **Play Mode / Edit Mode の完全分離**
    - [x] Edit Mode時はゲームロジック・物理演算を停止、Play Mode時はエディタ編集を制限しゲームロジックを優先する切り替え機構（シーンの一時保存・復元）の実装。
- [x] **完全データ駆動化への移行 (スクリプトのコンポーネント化)**
    - [x] C++の初期配置コード（ハードコード）を廃止し、自作スクリプトを `Component` 派生クラスとして実装。エディタ上からアタッチしてJSONで保存・復元できるモダンなUnityライクアーキテクチャへのリファクタリング（基盤完成）。
- [ ] **製品版（Releaseビルド）におけるエディタ完全削除の検証**
    - EditorModeマクロがオフの時に、1バイトも不要なコードが含まれないことの確認。

## 🚀 フェーズ6: ソロ制作向け・実戦用ゲームエンジンの完成
来月からの個人制作を強力にサポートするための、使い勝手向上と表現力強化機能群。
- [x] **自作スクリプト（Component）作成の効率化・自動化**
    - [x] スクリプトのインスペクター登録や Serialize/Deserialize を簡略化するマクロ（リフレクションの簡易実装）を導入。
- [x] **ゲームロジック・イベント連携の強化**
    - [x] コライダーの `OnCollisionEnter` コールバック対応、`Destroy`・`Instantiate` の整理。
- [x] **2D UI（キャンバス）システムの実装**
    - [x] `ButtonComponent` を実装。シーン遷移（フェード等のPostProcess連携）およびアニメーション演出に対応。
    - [x] `CanvasComponent` を実装。子要素のフェード等の一括管理。
- [x] **エディタ操作性の向上（本フェーズ追加分）**
    - [x] SceneView上での2D Sprite直接ピッキング（クリック選択）とアウトライン表示。
    - [x] Hierarchyパネルでのオブジェクト表示/非表示（Active）切替機能。
    - [x] SceneView上でのBlenderライクなエディタ専用カメラ操作の導入。
- [x] **マルチメディアコンポーネントのエディタ対応**
    - [x] `AudioSourceComponent` の追加と、インスペクターからの調整機能。
    - [x] `ParticleEmitterComponent` のパラメータリアルタイム調整対応。
- [x] **シーン遷移のデータ駆動化**
    - [x] `SceneManager::LoadScene("Title")` によるJSONからの直接遷移。

## 🧹 フェーズ7: エンジンの安定化と保守性の向上 (Stability & Maintenance)
- [x] **アプリケーション終了時のリソースリーク（LIVE_DEVICE）の解消**
    - [x] `RenderGraph` 等の遅延破棄と、各種Managerの確実なリソース解放。
    - [x] 無効なテクスチャアクセス時のGPUバリデーションエラーの回避処理（フォールバックテクスチャ）。
- [ ] **メモリ使用量のプロファイリングと最適化**
    - [ ] `TextureManager` および `ModelManager` の未使用リソース自動開放（LRUキャッシュ等）の検討。
- [ ] **製品版（Releaseビルド）におけるエディタ完全削除の検証**
    - [ ] EditorModeマクロがオフの時に、1バイトも不要なコードが含まれないことの確認。

## ✨ フェーズ8: AIおすすめの次期拡張プラン (Future Features)
表現力とエディタの完成度を一段階上げるための提案です。
- [ ] **ランタイムMSDF動的生成によるテキスト描画 (`TextComponent`)**
    - FreeType, msdfgen, stb_rect_pack を導入し、実行時に必要な文字だけを動的にMSDFテクスチャ化するシステム。
    - **【商用水準の安全対策】**
        - ロード時ウォームアップと既存の `ThreadPool` を活用した非同期生成タスクによる、ゲームプレイ中の処理落ち（スパイク）完全回避。
        - DirectX12 の Uploadヒープ・リソースバリアを用いた、フレーム開始時の安全なテクスチャ部分転送機構。
        - 2048x2048アトラス容量超過時の複数ページング（マルチテクスチャ）対応。
- [ ] **コライダーの視覚化と直感的なリサイズ操作**
    - SceneView上でコライダーのワイヤーフレームを描画し、直接ドラッグしてサイズ調整できる機能。
- [ ] **パーティクルエディタの充実化**
    - インスペクターからの直感的なスライダー操作とリアルタイムプレビューによるエフェクト作成環境の強化。
- [ ] **マテリアルエディタ＆シェーダバリアント**
    - Normal Map、Emission等のPBRパラメータをエディタで設定・即時反映する機能。
- [ ] **タイムライン/Tweenアニメーションシステム**
    - ボス登場演出やUIの複雑なアニメーションをコードレスで作成する仕組み。


