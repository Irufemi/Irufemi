# 個人制作：コンポーネント指向エディタ構築計画

## 🛠️ フェーズ1: コンポーネント指向アーキテクチャの基盤構築
従来のクラスベース（ObjClass等）の管理から脱却し、柔軟な組み立てを可能にするECSライクな基盤を作成する。
- [x] すべてのエンティティのベースとなる `GameObject` クラスの作成
- [x] `GameObject` にアタッチできる `Component` 基底クラスの作成
- [ ] 基本コンポーネント群（`TransformComponent`, `MeshRendererComponent` など）の実装
- [ ] 【将来的な負債解消】既存の `ObjClass` や `Sprite` が内部で Transform を抱え込む「Fat Class」状態を解消し、`TransformComponent` の座標データのみを使って描画する軽量なクラスへとリファクタリングする。

## 🖱️ フェーズ2: マウスクリックによるピッキング（Raycast）処理
エディタの「シーンビュー」上でオブジェクトを選択するための判定機構。
- [ ] `Camera` クラスに、画面上のマウス座標からワールド空間への Ray（光線）を生成する関数を追加
- [ ] `Collision` ユーティリティに Ray vs OBB, Ray vs AABB などの交差判定処理を実装
- [ ] 選択されたオブジェクトをハイライト（アウトライン描画等）する仕組みの追加

## 🎛️ フェーズ3: エディタ専用UIとギズモ（Gizmo）マネージャーの構築
現在ハードコードされているデバッグUIを汎用化し、直感的な操作パネルを作成する。
- [ ] `#ifdef EditorMode` でのみコンパイル・動作する `EditorManager` をエンジン側に新設
- [ ] 現在のシーン内の全 `GameObject` をツリー表示する「ヒエラルキー（Hierarchy）ウィンドウ」の実装
- [ ] 選択中のオブジェクトのコンポーネントパラメータ（位置・色・テクスチャ等）を編集する「インスペクター（Inspector）ウィンドウ」の実装
- [ ] オブジェクトを直接移動・回転・拡大縮小するための `ImGuizmo` を、任意の `GameObject` にアタッチして操作できる仕組みを構築

## 💾 フェーズ4: シーンのシリアライズ（保存と読み込み）機能
エディタ上で配置したオブジェクトや設定した数値をファイルに永続化する。
- [ ] `GameObject` と各 `Component` のパラメータを JSON 形式に変換（シリアライズ）するインターフェースの実装
- [ ] 作成したシーンファイル（.json）を保存、および実行時にパースして自動展開（ロード）する `SceneSerializer` クラスの作成

## 🔄 フェーズ5: メインループのモード分離（Play Mode / Edit Mode）
エディタ操作中とゲーム実行中の状態を分離する。
- [ ] エディタモード中（非再生時）はゲームロジックの `Update` を停止し、カメラ操作やオブジェクトの編集結果のみが画面に描画されるようにループ機構を整理